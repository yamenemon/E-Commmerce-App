enum MenuState { home, favourite, history, profile }
enum PAGES {
  MY_ORDER,
  ORDER_HISTORY,
  EDIT_PROFILE,
  MY_ADDRESS,
}

enum Method { POST, GET, PUT, DELETE, PATCH }

enum CART_STATUS {
  INCREMENT,
  REMOVE,
  DECREMENT,
}
